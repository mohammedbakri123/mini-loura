/**
 * Supplier identity. Stage 2 (Operational Model) will populate suppliers from
 * events / reference data.
 */
export interface Supplier {
  id: string;
  name: string;
  createdAt: string;
  updatedAt: string;
}
